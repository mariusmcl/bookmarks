#include <time.h>
#include <stdio.h>
#include <unistd.h>
#include <sys/time.h>
#include <stdlib.h>
#define long long int BIGMAN;

int main(int argc, char *argv[])
{
    int * bigboy = malloc(sizeof(int) * 1024 * 1000 * 10);
    // int * qstart = malloc(sizeof(int) * 1024);


    printf("yoloman\n");
}