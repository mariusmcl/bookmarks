#include <iostream>
#include <vector>
#include <string>
#include <sys/types.h>
#include <unistd.h>
#include <stdio.h>

using namespace std;

int main()
{
    // vector<string> msg {"Hello", "C++", "World", "from", "VS Code", "and the C++ extension!"};
    // int pid = fork()
    int pid = fork()
    
    cout << "123" << endl;

    /* for (const string& word : msg)
    {
        cout << word << " ";
    } */
    cout << endl;
}